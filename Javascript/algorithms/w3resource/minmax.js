// Write a JavaScript function that takes an array of numbers and finds the second lowest and second greatest numbers, respectively.

const minMaxSecond = arr => {
  let secondLeast = 0;
  let secondHighest = 0;

  for (let i=0; i < arr.length; i++) {

  }

  return [secondLeast, secondHighest]
}

console.log(minMaxSecond([3, 5, 7, 9, 10, 1, 4, 2]));